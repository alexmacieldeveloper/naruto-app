import Typography from '@mui/material/Typography'
import { Layout } from '../../components/Layout'

export const AkatsukiPage = () => {
    return (
        <Layout>
        <Typography variant="h1" color="initial">Akatsuki Page</Typography>

        </Layout>
    )
}